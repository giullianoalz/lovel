# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# Service worker output from vite-plugin-pwa in dev — regenerated on every run
dev-dist
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Environment variables (never commit these)
.env
.env.local
.env.production

# Claude Code local settings
.claude/settings.local.json

# Runtime uploads (server-side storage, not source)
server/uploads/

# Stray test artifacts
test.jpg

# One-off scratch/debug scripts and generated previews — never source.
# Kept on disk, out of the repo.
_tmp_*
server/scratch_*.cjs
server/enroll-burbuja.cjs
server/find-class.cjs
