{
  "name": "academy-management-api",
  "version": "1.0.0",
  "description": "Backend API for Academy Management System",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "(prisma migrate deploy || echo '⚠ migrate deploy skipped — DB may already be in sync') && node src/index.js",
    "postinstall": "prisma generate",
    "db:generate": "npx prisma generate",
    "db:migrate": "npx prisma migrate dev",
    "db:migrate:deploy": "npx prisma migrate deploy",
    "db:push": "npx prisma db push",
    "db:seed": "node prisma/seed.js",
    "db:studio": "npx prisma studio"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.105.0",
    "@prisma/client": "^6.9.0",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.0",
    "firebase-admin": "^13.4.0",
    "googleapis": "^173.0.0",
    "helmet": "^8.1.0",
    "lru-cache": "^11.5.1",
    "morgan": "^1.10.0",
    "multer": "^2.1.1",
    "node-cron": "^3.0.3",
    "prisma": "^6.9.0",
    "resend": "^6.17.1",
    "socket.io": "^4.8.0",
    "stripe": "^18.1.0",
    "zod": "^3.25.3"
  }
}
