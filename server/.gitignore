node_modules/
.env
*.log
dist/

# Database dumps. These carry student, medical and family records in the clear —
# never commit one.
backups/

# One-off admin scripts and their backups. Same reason as backups/ above — a
# billing-fix script here routinely dumps real family names, emails and
# balances to a JSON snapshot before it writes.
scratch/
